{
  "name": "@zero-path/audio-protocol",
  "version": "1.0.0",
  "private": true,
  "description": "ESP32 与 macOS 共用的通话音频帧协议",
  "scripts": {
    "build": "../../scripts/native.sh build",
    "lint": "../../scripts/native.sh lint",
    "test": "../../scripts/native.sh test"
  }
}